# Source-control transport policy lives in local .env; never put tokens here.
# GitHub: gh auth git-credential with GIT_ASKPASS unset.
# git.3k1h.com: set GIT_ASKPASS only for that GitLab command.
# Do not commit filled secret values.


# Optional: point scripts at a non-default env file.
