schema: harness.project-manifest.v1
project_slug: harness-starter
runtime:
  package: harness-runtime
  version: "0.1.0"
contracts:
  execution_receipt: execution-receipt.v1
policy_refs:
  - .harness/project/docs/decisions/hn_harness_runtime_externalization.md
state:
  source_tree: forbidden
  receipt_storage: explicit-HARNESS_STATE_DIR-only
deterministic_runner:
  worktree_cwd: explicit-clean-git-root-required
  environment: constrained-no-caller-inheritance
  analyst_input: verified-receipt-only
